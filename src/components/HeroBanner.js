import React from "react";
import { Box, Stack, Typography, typography } from "@mui/material";

function HeroBanner() {
    return(
        <Box>
            <Typography color="#FF2625"
            fontWeight="660" fontsize="26px">
                Fitness Club
            </Typography>
        </Box>
    );
}

export default HeroBanner;